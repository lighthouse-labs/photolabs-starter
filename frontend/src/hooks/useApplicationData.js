const App = () => {
  const {
    state,//entire state of the application
    onPhotoSelect,//used when the user selects a photo
    updateToFavPhotoIds,// used to set the favourite photos
    onLoadTopic,
    onClosePhotoDetailsModal,//used to close the modal
  } = useApplicationData();

  return (
    // React components
  );
}